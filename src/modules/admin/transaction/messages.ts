import BaseResponse from "../../BaseResponse";

class MESSAGES extends BaseResponse {

    NAME_ALREADY_EXIST: string = "Name already exit";
    CODE_ALREADY_EXIST: string = "Code already exit";
}

export default new MESSAGES();